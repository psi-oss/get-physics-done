# Phase 08 Packet BT-035

- reconstruction_scope: `covered_live_phase09_subset`
- packet_ids: `PK-BT-035-01, PK-BT-035-01`
- anchor_candidate_ids: `VF-01-almheiri-r02-001, VF-01-almheiri-r02-009, VF-02-maloney-r03-004, VF-13-usatyuk-r03-002, VF-17-antonini-r02-006, VF-17-antonini-r03-006`
- interface_classes: `cli`

## Assertions

### PK-BT-035-01

- fixture: `FX-resume-handoff-positive`
- workspace: `tests/fixtures/handoff-bundle/resume-handoff/positive/workspace`
- commands: `['gpd progress', 'gpd state snapshot', 'gpd resume']`
- expected: `health` should expose the same continuation state visible from `resume`.
- actual: `gpd progress` again reports all phases `05-08` as `Pending` with `0` plans and `0` summaries, while `gpd state snapshot` still says `Current Phase: 05` and `Status: Researching`, and `gpd resume` still carries `R-08-verdict` plus `HANDOFF.md` as the active recovery target.

### PK-BT-035-01

- fixture: `FX-resume-handoff-mutation`
- workspace: `tests/fixtures/handoff-bundle/resume-handoff/mutation/workspace`
- commands: `['gpd progress', 'gpd state snapshot', 'gpd resume']`
- expected: `health` should expose the same continuation state visible from `resume`.
- actual: `gpd progress` again reports all phases `05-08` as `Pending` with `0` plans and `0` summaries, while `gpd state snapshot` still says `Current Phase: 05` and `Status: Researching`, and `gpd resume` still carries `R-08-verdict` plus `HANDOFF.md` as the active recovery target.
