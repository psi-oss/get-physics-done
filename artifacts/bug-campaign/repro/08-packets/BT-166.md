# Phase 08 Packet BT-166

- reconstruction_scope: `covered_live_phase09_subset`
- packet_ids: `PK-BT-166-01, PK-BT-166-01`
- anchor_candidate_ids: `VF-01-almheiri-r02-001, VF-01-almheiri-r02-009, VF-02-maloney-r03-004, VF-13-usatyuk-r03-002, VF-17-antonini-r02-006, VF-17-antonini-r03-006`
- interface_classes: `cli`

## Assertions

### PK-BT-166-01

- fixture: `FX-resume-handoff-positive`
- workspace: `tests/fixtures/handoff-bundle/resume-handoff/positive/workspace`
- commands: `['gpd phase list --type notes', 'gpd phase list --type plan']`
- expected: `roadmap_analyze` should agree with `state_snapshot` and the accepted `canonical_state_snapshot` state source about the same workspace facts.
- actual: `gpd phase list --type notes` and `gpd phase list --type plan` both return the same four `NOTES.md` filenames, so the type filter is not discriminating in this workspace.

### PK-BT-166-01

- fixture: `FX-resume-handoff-mutation`
- workspace: `tests/fixtures/handoff-bundle/resume-handoff/mutation/workspace`
- commands: `['gpd phase list --type notes', 'gpd phase list --type plan']`
- expected: `roadmap_analyze` should agree with `state_snapshot` and the accepted `canonical_state_snapshot` state source about the same workspace facts.
- actual: `gpd phase list --type notes` and `gpd phase list --type plan` both return the same four `NOTES.md` filenames, so the type filter is not discriminating in this workspace.
