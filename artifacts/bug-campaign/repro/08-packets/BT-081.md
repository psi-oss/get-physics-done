# Phase 08 Packet BT-081

- reconstruction_scope: `covered_live_phase09_subset`
- packet_ids: `PK-BT-081-01, PK-BT-081-01`
- anchor_candidate_ids: `VF-10-sorce-r01-008, VF-17-antonini-r02-002, VF-17-antonini-r02-006`
- interface_classes: `cli`

## Assertions

### PK-BT-081-01

- fixture: `FX-plan-only-positive`
- workspace: `tests/fixtures/handoff-bundle/plan-only/positive/workspace`
- commands: `['gpd verify phase 01', 'gpd verify phase 05', 'gpd phase index', 'gpd phase validate-waves']`
- expected: `summary_extract` should classify phase content consistently with the accepted phase artifact inventory.
- actual: Both `gpd verify phase 01` and `gpd verify phase 05` return `complete = True` with `plan_count = 0` and `summary_count = 0`; `gpd phase index` and `gpd phase validate-waves` also mark these empty phases as valid, while `gpd health` simultaneously warns that the same phase directories are empty.

### PK-BT-081-01

- fixture: `FX-plan-only-mutation`
- workspace: `tests/fixtures/handoff-bundle/plan-only/mutation/workspace`
- commands: `['gpd verify phase 01', 'gpd verify phase 05', 'gpd phase index', 'gpd phase validate-waves']`
- expected: `summary_extract` should classify phase content consistently with the accepted phase artifact inventory.
- actual: Both `gpd verify phase 01` and `gpd verify phase 05` return `complete = True` with `plan_count = 0` and `summary_count = 0`; `gpd phase index` and `gpd phase validate-waves` also mark these empty phases as valid, while `gpd health` simultaneously warns that the same phase directories are empty.
