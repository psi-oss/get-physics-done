# Phase 08 Packet BT-071

- reconstruction_scope: `covered_live_phase09_subset`
- packet_ids: `PK-BT-071-01, PK-BT-071-01`
- anchor_candidate_ids: `VF-01-almheiri-r01-008`
- interface_classes: `race`

## Assertions

### PK-BT-071-01

- fixture: `FX-resume-recent-noise-positive`
- workspace: `tests/fixtures/handoff-bundle/resume-recent-noise/positive/workspace`
- commands: `['gpd state update']`
- expected: `resume_recent` should satisfy the `mutation_ordering` contract against `phase_and_state_contract`.
- actual: Parallel `gpd state update` calls are not safe for dependent transitions: when I tried that once, the second command still saw the old state and failed the transition check.

### PK-BT-071-01

- fixture: `FX-resume-recent-noise-mutation`
- workspace: `tests/fixtures/handoff-bundle/resume-recent-noise/mutation/workspace`
- commands: `['gpd state update']`
- expected: `resume_recent` should satisfy the `mutation_ordering` contract against `phase_and_state_contract`.
- actual: Parallel `gpd state update` calls are not safe for dependent transitions: when I tried that once, the second command still saw the old state and failed the transition check.
