# Phase 08 Packet BT-122

- reconstruction_scope: `covered_live_phase09_subset`
- packet_ids: `PK-BT-122-01, PK-BT-122-01`
- anchor_candidate_ids: `VF-01-almheiri-r02-010, VF-09-deboer-r03-012, VF-13-usatyuk-r02-001`
- interface_classes: `cli`

## Assertions

### PK-BT-122-01

- fixture: `FX-summary-missing-return-positive`
- workspace: `tests/fixtures/handoff-bundle/summary-missing-return/positive/workspace`
- commands: `['gpd calculation list', 'gpd question list', 'gpd approximation list/check', 'gpd uncertainty list']`
- expected: `unknown_surface` should satisfy the `provenance_propagation` contract against `canonical_result_registry`.
- actual: `gpd calculation list`, `gpd question list`, `gpd approximation list/check`, and `gpd uncertainty list` confirm that the state driving `gpd suggest` is real: 1 active calculation, 4 open questions, 2 approximations still `unchecked`, and 1 propagated uncertainty entry.

### PK-BT-122-01

- fixture: `FX-summary-missing-return-mutation`
- workspace: `tests/fixtures/handoff-bundle/summary-missing-return/mutation/workspace`
- commands: `['gpd calculation list', 'gpd question list', 'gpd approximation list/check', 'gpd uncertainty list']`
- expected: `unknown_surface` should satisfy the `provenance_propagation` contract against `canonical_result_registry`.
- actual: `gpd calculation list`, `gpd question list`, `gpd approximation list/check`, and `gpd uncertainty list` confirm that the state driving `gpd suggest` is real: 1 active calculation, 4 open questions, 2 approximations still `unchecked`, and 1 propagated uncertainty entry.
