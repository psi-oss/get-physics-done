"""Shared adapter-owned runtime defaults."""

from __future__ import annotations

AUTO_DISCOVERED_TOOL_DEFAULTS = frozenset({"task"})
SHELL_FENCE_LANGUAGES = frozenset({"bash", "sh", "shell", "zsh"})

