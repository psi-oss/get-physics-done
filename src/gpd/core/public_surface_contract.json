{
  "schema_version": 1,
  "beginner_onboarding": {
    "hub_url": "https://github.com/psi-oss/get-physics-done/tree/main/docs",
    "preflight_requirements": [
      "One supported runtime is already installed and can open from your normal terminal.",
      "Node.js 20+ is available in that same terminal.",
      "Python 3.11+ with the standard `venv` module is available there too."
    ],
    "caveats": [
      "GPD is not a standalone app.",
      "GPD does not install your runtime for you.",
      "GPD does not include model access, billing, or API credits.",
      "This hub is the beginner path, not the full reference.",
      "If evidence, references, or artifacts are missing, say so explicitly; GPD should not invent them."
    ],
    "startup_ladder": [
      "help",
      "start",
      "tour",
      "new-project / map-research",
      "resume-work"
    ]
  },
  "local_cli_bridge": {
    "commands": [
      "gpd --help",
      "gpd doctor",
      "gpd validate unattended-readiness --runtime <runtime> --autonomy <mode>",
      "gpd permissions status --runtime <runtime> --autonomy <mode>",
      "gpd permissions sync --runtime <runtime> --autonomy <mode>",
      "gpd resume",
      "gpd resume --recent",
      "gpd observe execution",
      "gpd cost",
      "gpd presets list",
      "gpd validate plan-preflight <PLAN.md>",
      "gpd integrations status wolfram"
    ],
    "named_commands": {
      "help": "gpd --help",
      "doctor": "gpd doctor",
      "unattended_readiness": "gpd validate unattended-readiness --runtime <runtime> --autonomy <mode>",
      "permissions_status": "gpd permissions status --runtime <runtime> --autonomy <mode>",
      "permissions_sync": "gpd permissions sync --runtime <runtime> --autonomy <mode>",
      "resume": "gpd resume",
      "resume_recent": "gpd resume --recent",
      "observe_execution": "gpd observe execution",
      "cost": "gpd cost",
      "presets_list": "gpd presets list",
      "plan_preflight": "gpd validate plan-preflight <PLAN.md>",
      "integrations_status_wolfram": "gpd integrations status wolfram"
    },
    "terminal_phrase": "from your normal terminal",
    "purpose_phrase": "install/readiness checks, typed command validation, permissions, observability, diagnostics, recovery, cost, presets, and shared Wolfram integration",
    "install_local_example": "gpd install <runtime> --local",
    "doctor_local_command": "gpd doctor --runtime <runtime> --local",
    "doctor_global_command": "gpd doctor --runtime <runtime> --global",
    "validate_command_context_command": "gpd validate command-context gpd:<name>"
  },
  "post_start_settings": {
    "primary_sentence": "After your first successful start or later, use the runtime `settings` command to review autonomy, workflow defaults, model-cost posture, runtime permission sync, and preset/tier overrides.",
    "default_sentence": "The safest starting point is `review` plus runtime defaults. Favor scientific rigor and explicit uncertainty over agreement-seeking, and keep missing evidence or artifacts explicit instead of inventing them."
  },
  "resume_authority": {
    "durable_authority_phrase": "`state.json.continuation` is the durable authority",
    "public_vocabulary_intro": "Canonical continuation fields define the public resume vocabulary",
    "public_fields": [
      "active_resume_kind",
      "active_resume_origin",
      "active_resume_pointer",
      "active_bounded_segment",
      "derived_execution_head",
      "active_resume_result",
      "continuity_handoff_file",
      "recorded_continuity_handoff_file",
      "missing_continuity_handoff_file",
      "resume_candidates"
    ]
  },
  "recovery_ladder": {
    "title": "Recovery ladder",
    "local_snapshot_command": "gpd resume",
    "local_snapshot_phrase": "the current-workspace read-only recovery snapshot",
    "cross_workspace_command": "gpd resume --recent",
    "cross_workspace_phrase": "find the workspace first",
    "resume_phrase": "continue inside that workspace",
    "next_phrase": "the fastest next command",
    "pause_phrase": "an explicit handoff to restore, projected from canonical continuation"
  }
}
