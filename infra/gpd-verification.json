{
  "name": "gpd-verification",
  "description": "GPD physics verification support tools. They expose check metadata, static triage, contract-aware check execution, dimensional-analysis helpers, domain and bundle-specific checklists, limiting-case prompts, symmetry-check prompts, and coverage gap analysis; MCP results do not by themselves grant final scientific verification status. Contract-aware requests are closed and schema-driven; full rules live on the `contract` input schema. Proof checks require authoritative contract payloads. Missing grounding, evidence, or proof artifacts stay explicit. Use `suggest_contract_checks(...)` before `run_contract_check(...)`. Only contract-payload enum case drift is recoverable; observed enums must match source evidence exactly.",
  "transport": "stdio",
  "command": "gpd-mcp-verification",
  "args": [],
  "env": {
    "LOG_LEVEL": "${LOG_LEVEL:-WARNING}"
  },
  "capabilities": [
    "run_check",
    "run_contract_check",
    "suggest_contract_checks",
    "get_checklist",
    "get_bundle_checklist",
    "dimensional_check",
    "limiting_case_check",
    "symmetry_check",
    "get_verification_coverage",
    "conservation_check",
    "equation_check",
    "series_check",
    "ode_check",
    "tensor_check",
    "integral_check",
    "commutator_check",
    "pde_check",
    "matrix_check"
  ],
  "registry_prefix": "gpd_verification",
  "prerequisites": [
    "Install GPD before enabling built-in MCP servers."
  ],
  "health_check": {
    "probe_kind": "schema_valid",
    "tool": "get_checklist",
    "input": {
      "domain": "qft"
    },
    "expect": "contains Ward identities"
  },
  "alternatives": {
    "python_module": {
      "command": "${GPD_PYTHON}",
      "args": [
        "-m",
        "gpd.mcp.servers.verification_server"
      ],
      "notes": "Replace `${GPD_PYTHON}` with a Python >=3.11 interpreter that has GPD installed."
    }
  }
}
