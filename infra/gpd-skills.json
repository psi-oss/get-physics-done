{
  "name": "gpd-skills",
  "description": "GPD skill discovery and routing for workflow prompts; never fabricate missing outputs.",
  "source_of_truth": "src/gpd/mcp/builtin_servers.py",
  "transport": "stdio",
  "command": "gpd-mcp-skills",
  "args": [],
  "env": {},
  "capabilities": [
    "list_skills",
    "get_skill",
    "route_skill",
    "get_skill_index"
  ],
  "registry_prefix": "gpd_skills",
  "prerequisites": [
    "Install GPD before enabling built-in MCP servers."
  ],
  "health_check": {
    "tool": "list_skills",
    "input": {},
    "expect": "contains gpd-execute-phase and gpd-research-phase"
  },
  "alternatives": {
    "python_module": {
      "command": "${GPD_PYTHON}",
      "args": [
        "-m",
        "gpd.mcp.servers.skills_server"
      ],
      "notes": "Replace `${GPD_PYTHON}` with a Python >=3.11 interpreter that has GPD installed."
    }
  }
}
