{
  "name": "gpd-skills",
  "description": "GPD skill discovery and routing. Tools for listing, retrieving, auto-routing, and indexing GPD workflow skills for agent prompt injection. Treat missing evidence or artifacts as missing, blocked, failed, or inconclusive; never fabricate fallback outputs.",
  "transport": "stdio",
  "command": "gpd-mcp-skills",
  "args": [],
  "env": {
    "LOG_LEVEL": "${LOG_LEVEL:-WARNING}"
  },
  "capabilities": [
    "list_skills",
    "get_skill",
    "route_skill",
    "get_skill_index"
  ],
  "registry_prefix": "gpd_skills",
  "prerequisites": [
    "Install GPD before enabling built-in MCP servers."
  ],
  "health_check": {
    "tool": "list_skills",
    "input": {},
    "expect": "contains gpd-execute-phase and gpd-research-phase"
  },
  "alternatives": {
    "python_module": {
      "command": "${GPD_PYTHON}",
      "args": [
        "-m",
        "gpd.mcp.servers.skills_server"
      ],
      "notes": "Replace `${GPD_PYTHON}` with a Python >=3.11 interpreter that has GPD installed."
    }
  }
}
